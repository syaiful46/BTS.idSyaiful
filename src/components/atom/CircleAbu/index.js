import React from 'react';
import {StyleSheet, View, Text} from 'react-native';

const CircleAbu = () => {
  return <View style={styles.dalam} />;
};

export default CircleAbu;

const styles = StyleSheet.create({
  dalam: {
    backgroundColor: '#C4C4C4',
    borderRadius: 20,
    width: 15,
    height: 15,
    marginTop: 2,
    marginHorizontal: 3,
  },
});
